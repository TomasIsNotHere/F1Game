
F1 race game - 
            W - speed up
            S - slow down / move back
            A/D - left/right 
            esc - exit
            
"""
for creator of computer car:
path creator
    if event.type == pygame.MOUSEBUTTONDOWN:
        pos = pygame.mouse.get_pos()
        green_car.path.append(pos)
        print(green_car.path)
"""